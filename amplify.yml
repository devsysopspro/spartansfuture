version: 1
backend:
  phases:
    build:
      commands:
        - cd public
        - npm install
  artifacts:
    baseDirectory: public
    files:
      - 'src/**/*'
      - 'package.json'
      - 'package-lock.json'
  cache:
    paths:
      - public/node_modules/**/* 