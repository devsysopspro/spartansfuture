version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd public
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: public/build
    files:
      - '**/*'
  cache:
    paths:
      - public/node_modules/**/* 